{
  "name": "mods-manager",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^6.5.3",
    "flow-bin": "^0.80.0",
    "memoize-one": "^3.1.1",
    "npm": "^5.10.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "^1.1.5"
  },
  "scripts": {
    "start": "NODE_PATH=src,src/components HTTPS=true react-scripts start",
    "build": "NODE_PATH=src,src/components react-scripts build",
    "test": "NODE_PATH=src,src/components CI=true react-scripts test --env=jsdom --coverage",
    "eject": "NODE_PATH=src,src/components react-scripts eject",
    "lint": "node node_modules/eslint/bin/eslint.js --config node_modules/eslint-config-react-app/index.js",
    "flow": "flow"
  },
  "devDependencies": {
    "webpack": "^3.12.0"
  }
}
